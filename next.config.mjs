/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Optional but good practice
  /* standard options go here (like images, rewrites, redirects) */
};

export default nextConfig;